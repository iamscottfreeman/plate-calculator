import React from 'react';

import { fonts } from '@/app/fonts';

const MoreWeight = () => {
  return <div className={`${fonts.header.className} uppercase text-xl mt-4`}>Add more weight!</div>;
};

export default MoreWeight;
